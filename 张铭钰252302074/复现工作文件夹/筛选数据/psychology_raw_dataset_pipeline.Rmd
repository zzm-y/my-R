---
title: "Psychology文章Raw Dataset查找流程"
author: "Data Analysis"
date: "`r Sys.Date()`"
output:
  ioslides_presentation:
    widescreen: true
    smaller: true
    transition: faster
  revealjs::revealjs_presentation:
    theme: white
    transition: slide
    self_contained: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)
library(dplyr)
library(knitr)
library(kableExtra)
```

## 目标

**从多个CSV文件中找到psychology领域的文章及其原始数据集**

- 输入：4个CSV文件
- 处理：数据筛选、合并、回查
- 输出：有raw dataset可用的psychology文章列表

---

## 数据文件概览

| 文件名 | 主要内容 | 关键字段 |
|--------|---------|---------|
| `score_rr-project_status.csv` | RR项目状态 | `paper_id`, `rr_id`, `field` |
| `pr_outcomes.csv` | PR结果 | `paper_id`, `data_available` |
| `orig-material_availability.csv` | 原始材料可用性 | `pdf_filename`, `pr_data_available...11` |
| `orig-material_author-outreach.csv` | 作者联系记录 | `pdf_filename`, `paper_id` |

---

## Pipeline流程图

```
score_rr-project_status.csv
        │
        │ filter(field == "psychology")
        ▼
psychology paper_id / rr_id list
        │
        │ left_join by paper_id
        ▼
pr_outcomes.csv
        │
        ├─ data_available
        ├─ data_location_description
        ├─ data_notes
        └─ code_available
            │
            ├─ 信息够：直接拿链接
            └─ 信息不够：回查原始文件
```

---

## 步骤1：筛选Psychology文章

```{r step1}
# 读取数据
rr_status <- read.csv("score_rr-project_status.csv", stringsAsFactors = FALSE)

# 筛选psychology领域
psychology_papers <- rr_status %>% 
  filter(field == "psychology")

# 查看各领域的文章数量
field_counts <- rr_status %>%
  group_by(field) %>%
  summarise(count = n()) %>%
  arrange(desc(count))

kable(field_counts, col.names = c("领域", "文章数"))
```

**共140篇psychology文章**

---

## 步骤2：Join pr_outcomes.csv

```{r step2}
# 读取PR结果数据
pr_outcomes <- read.csv("pr_outcomes.csv", stringsAsFactors = FALSE)

# Left join by paper_id
merged_data <- psychology_papers %>%
  left_join(pr_outcomes, by = "paper_id")

# 查看关键字段
key_fields <- merged_data %>%
  select(paper_id, rr_id, data_available, 
         data_location_description, data_notes) %>%
  #head(5)
  filter(data_available == "Yes")

write.csv(key_fields, "paper_available.csv", row.names = FALSE)
#%>8
# head(5)

kable(key_fields, col.names = c("Paper ID", "RR ID", "数据可用", 
                                 "数据位置描述", "备注"))
```

---

## 步骤3：检查数据可用性

```{r step3}
# 查看有数据可用的文章
has_data <- merged_data %>% 
  filter(data_available == "Yes")

cat("有raw dataset可用的记录数：", nrow(has_data), "\n")
cat("对应的唯一paper_id数：", length(unique(has_data$paper_id)), "\n")

# 数据位置分布
location_summary <- merged_data %>%
  filter(data_available == "Yes") %>%
  mutate(location_type = case_when(
    grepl("OSF", data_location_description) ~ "OSF平台",
    grepl("journal|Science Direct|Wiley|Sage", data_location_description) ~ "期刊网站",
    grepl("author|website", data_location_description) ~ "作者网站",
    grepl("GitHub", data_location_description) ~ "GitHub",
    TRUE ~ "其他"
  )) %>%
  group_by(location_type) %>%
  summarise(count = n()) %>%
  arrange(desc(count))

kable(location_summary, col.names = c("数据位置", "数量"))
```

---

## 步骤4：信息不足时回查

```{r step4}
# 读取原始材料文件
orig_availability <- read.csv("orig-material_availability.csv", 
                              stringsAsFactors = FALSE)
orig_outreach <- read.csv("orig-material_author-outreach.csv", 
                          stringsAsFactors = FALSE)

# 示例：查看paper_id "8R9d"的详细信息
example_paper <- "8R9d"

# 从pr_outcomes获取信息
example_info <- pr_outcomes %>%
  filter(paper_id == example_paper) %>%
  select(pdf_filename, data_available, data_location_description, 
         data_notes, code_available)

kable(example_info, col.names = c("文件名", "数据可用", "数据位置", 
                                   "备注", "代码可用"))
```

---

## 实际案例：paper_id = "8R9d"

```{r case_study}
# 回查orig-material_availability.csv
example_pdf <- unique(example_info$pdf_filename)

orig_info <- orig_availability %>%
  filter(pdf_filename == example_pdf) %>%
  select(pdf_filename, pr_data_available...11, pr_data_location_description)

kable(orig_info, col.names = c("文件名", "原始数据可用", "原始数据位置描述"))
```

**验证结果**：两个数据源都确认数据可用，位置为OSF平台

---

## 完整Pipeline代码

```{r full_pipeline, eval=FALSE}
# 完整的数据处理流程
result <- rr_status %>%
  filter(field == "psychology") %>%
  left_join(pr_outcomes, by = "paper_id") %>%
  filter(data_available == "Yes") %>%
  left_join(orig_availability, by = "pdf_filename") %>%
  left_join(orig_outreach, by = "pdf_filename")

# 保存结果
write.csv(result, "psychology_raw_dataset_final.csv", row.names = FALSE)

# 查看摘要
summary_result <- result %>%
  group_by(data_available) %>%
  summarise(count = n())

print(summary_result)
```

---

## 关键发现

1. **140篇** psychology文章中筛选出**52条**有可用数据的PR记录
2. 主要托管平台：**OSF** (60%)，其次是期刊官网 (30%)
3. 数据格式多样：CSV, SAV (SPSS), Excel, R数据文件
4. 部分数据需要**机构访问权限**
5. 有些只包含**部分实验数据**

**建议**：优先使用OSF平台的数据，因其提供永久存储和版本控制

---

## 输出文件

- `psychology_raw_dataset.csv` - 有数据可用的文章列表
- `psychology_raw_dataset_final.csv` - 完整回查后的数据
- `psychology_raw_dataset_pipeline.Rmd` - 本演示文档

**下一步**：
- 访问OSF链接下载原始数据
- 检查数据完整性和代码可复现性
- 进行元分析或数据整合


